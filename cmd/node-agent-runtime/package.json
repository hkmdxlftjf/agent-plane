{
  "name": "agent-plane-node-runtime",
  "version": "1.0.0",
  "private": true,
  "description": "A reference Agent Plane runtime built on the Vercel AI SDK (see docs/runtime-protocol.md). Alternative to cmd/agent-runtime (Go) for models/providers the AI SDK handles better out of the box (reasoning-content separation across providers, multi-provider gateway).",
  "type": "module",
  "main": "src/main.js",
  "scripts": {
    "start": "node src/main.js",
    "test": "node --test test/*.test.mjs"
  },
  "dependencies": {
    "ai": "^5.0.240",
    "@ai-sdk/openai-compatible": "^1.0.48"
  },
  "overrides": {
    "undici": "^7.29.0"
  }
}
