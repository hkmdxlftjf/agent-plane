apiVersion: core.cognet.io/v1alpha1
kind: Agent
metadata:
  name: support-agent
spec:
  description: Demo customer-support agent — model + prompt + MCP tool + skill.
  modelRef:
    name: llm-model
  promptRef:
    name: support-prompt
  toolRefs:
    - name: order-lookup
  skillRefs:
    - name: refund-handling
