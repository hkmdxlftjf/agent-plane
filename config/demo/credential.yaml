apiVersion: core.hkmdxlftjf.io/v1alpha1
kind: Credential
metadata:
  name: llm-cred
spec:
  description: API key for the demo model endpoint (Secret created by hack/demo.sh).
  secretRef:
    name: llm-secret
    key: api-key
