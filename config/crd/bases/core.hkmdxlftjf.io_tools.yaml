---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: tools.core.hkmdxlftjf.io
spec:
  group: core.hkmdxlftjf.io
  names:
    kind: Tool
    listKind: ToolList
    plural: tools
    singular: tool
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.type
      name: Type
      type: string
    - jsonPath: .spec.version
      name: Version
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Tool is the Schema for the tools API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of Tool
            properties:
              agentRef:
                description: |-
                  agentRef, for type=mcp, references another Agent to consult instead of an
                  MCPServer. The target must set spec.expose; the Registry resolves this to
                  that Agent's peer endpoint.

                  This is how cross-repository work is expressed: an Agent bound to one
                  repository asks an Agent bound to another, rather than mounting a second
                  working tree. Because the edge is an ordinary Tool, Policy and ToolPolicy
                  govern it — denying this Tool severs the link, with no separate
                  agent-to-agent authorization model to keep in sync.

                  Exactly one of mcpServerRef or agentRef may be set.
                properties:
                  name:
                    description: name is the metadata.name of the referenced resource.
                    minLength: 1
                    type: string
                required:
                - name
                type: object
              description:
                description: |-
                  description explains what the tool does, so a model can decide when to
                  call it. Surfaced to runtimes as the tool-call description.
                type: string
              endpoint:
                description: |-
                  endpoint is the address the runtime uses to reach the tool: the tool's own
                  URL for type=http, and for type=mcp the fallback used when neither
                  mcpServerRef nor agentRef resolves an in-cluster address.
                type: string
              inputSchema:
                description: inputSchema is a JSON Schema for the tool's input arguments.
                type: object
                x-kubernetes-preserve-unknown-fields: true
              mcpServerRef:
                description: mcpServerRef, for type=mcp, references the MCPServer
                  exposing this tool.
                properties:
                  name:
                    description: name is the metadata.name of the referenced resource.
                    minLength: 1
                    type: string
                required:
                - name
                type: object
              mcpToolName:
                description: |-
                  mcpToolName, for type=mcp, is the tool name to invoke on the MCP server
                  when it differs from this resource's metadata.name.
                type: string
              outputSchema:
                description: outputSchema is a JSON Schema for the tool's result.
                type: object
                x-kubernetes-preserve-unknown-fields: true
              timeoutSeconds:
                description: timeoutSeconds bounds a single tool invocation.
                format: int32
                minimum: 1
                type: integer
              type:
                description: type is the invocation mechanism for this tool.
                enum:
                - http
                - mcp
                type: string
              version:
                description: version is the semantic version of the tool contract.
                type: string
            required:
            - type
            type: object
          status:
            description: status defines the observed state of Tool
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              healthy:
                description: healthy reflects the last observed health probe result,
                  if any.
                type: boolean
              observedGeneration:
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
