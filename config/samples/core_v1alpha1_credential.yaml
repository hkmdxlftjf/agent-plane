apiVersion: core.cognet.io/v1alpha1
kind: Credential
metadata:
  name: anthropic-key
spec:
  description: API key for the hosted Anthropic model endpoint.
  secretRef:
    name: anthropic-secret
    key: api-key
