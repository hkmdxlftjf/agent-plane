apiVersion: core.cognet.io/v1alpha1
kind: Policy
metadata:
  name: support-policy
spec:
  models:
    allow:
      - claude-opus
  memory:
    allow:
      - session-memory
  tools:
    deny:
      - dangerous-refund-all
